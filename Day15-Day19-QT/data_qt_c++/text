## 整体架构说明
1. `CarData.h / CarData.cpp`：纯 C++ 业务类，存储车速、电量，提供数据读写、变更信号
2. `main.cpp`：将 C++ 车辆对象注册到全局 QML 上下文，让界面能访问 C++ 数据
3. `main.qml`：QML 界面，展示 C++ 数据、滑块修改 C++ 车速，数据双向同步
4. 核心技术点：`Q_OBJECT`、`Q_PROPERTY`、信号通知、全局上下文注册


代码示例 完整数据流转逻辑：---------------------------------------------------------

## 场景 1：C++ 数据变更 → QML 界面自动刷新
1. C++ 调用 `setSpeed(80)` 修改内部`m_speed`
2. 执行 `emit speedChanged(80)` 发送变更信号
3. QML 中绑定了`g_car.speed`的所有控件自动收到信号，刷新文字显示

## 场景 2：QML 界面操作 → 修改 C++ 后端数据
1. 用户拖动滑块，触发`onValueChanged`
2. QML 调用全局 C++ 对象接口 `g_car.setSpeed(新数值)`
3. C++ 内部做业务合法性校验，合法则更新成员变量，发送信号回刷界面

## 场景 3：只读数据（电量 battery）
QML 只能读取`g_car.battery`，没有写接口，无法修改，适合固定参数。## 场景 1：C++ 数据变更 → QML 界面自动刷新
1. C++ 调用 `setSpeed(80)` 修改内部`m_speed`
2. 执行 `emit speedChanged(80)` 发送变更信号
3. QML 中绑定了`g_car.speed`的所有控件自动收到信号，刷新文字显示

## 场景 2：QML 界面操作 → 修改 C++ 后端数据
1. 用户拖动滑块，触发`onValueChanged`
2. QML 调用全局 C++ 对象接口 `g_car.setSpeed(新数值)`
3. C++ 内部做业务合法性校验，合法则更新成员变量，发送信号回刷界面

## 场景 3：只读数据（电量 battery）
QML 只能读取`g_car.battery`，没有写接口，无法修改，适合固定参数。


核心关键知识点拆解（基础重点）：---------------------------------------------------------

### `Q_OBJECT` 宏
- 作用：开启 Qt 元对象系统，编译器生成信号槽、Q_PROPERTY 的底层代码
- 坑：**所有要和 QML 交互的 C++ 类第一行必须写**，遗漏直接编译报错，找不到信号 / 属性

### 2. `Q_PROPERTY` 三要素（读写 + 通知）
1. `READ 函数`：QML 读取数据时调用
2. `WRITE 函数`：QML 修改数据时调用（只读属性可省略，加`CONSTANT`）
3. `NOTIFY 信号`：数据修改后发送，驱动 UI 自动刷新，实时同步

### 3. `setContextProperty("g_car", &carBiz)`
- 作用：将 C++ 实例暴露为 QML 全局变量，整个 qml 文件直接通过`g_car`访问 C++ 的属性和接口
- 第一个参数：QML 中使用的变量名，自定义；第二个参数：C++ 对象地址

### 4. 双向绑定本质
QML 读取`g_car.speed` → 底层调用 C++`getSpeed()`
QML 写入`g_car.setSpeed(val)` → 底层调用 C++`setSpeed(val)`


高频踩坑（规避）：---------------------------------------------------------

1. C++ 类忘记 `Q_OBJECT` 宏 → 编译提示未定义引用 `vtable`
2. `Q_PROPERTY` 缺少 NOTIFY 信号 → C++ 改数据，界面不会自动刷新
3. setSpeed 内部不做判等 `if(m_speed != newSpeed)` → 重复发送信号，界面频繁重绘卡顿
4. main.cpp 忘记注册 C++ 对象到上下文 → QML 使用`g_car`提示未定义变量
5. 信号函数只声明、不写实现，不需要加 {}，写了会编译报错