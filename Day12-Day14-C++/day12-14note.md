
## 学习 C++，关键是要理解概念，而不应过于深究语言的技术细节，作为面向对象的语言，为了能更有效率地设计和实现新系统，以及维护旧系统，C++支持多种编程风格。可以使用 Fortran、C、Smalltalk 等任意一种语言的编程风格来编写代码。
## 主要的 C++ 编译器的制造商都支持 ANSI 标准。编写的代码在 Mac、UNIX、Windows、Alpha 计算机上都能通过编译

# Day08 学习日志
很重要：🌟✅
    C++
        |——数据类型（C++新增）
            |——bool    ，布尔类型true 或 false，表示真或假，字节大小1字节
            |——wchar_t ，宽字符类型，用于存储 Unicode 字符，2或4字节，取决于平台，用于中文
            |——char16_t ，16 位 Unicode 字符类型（C++11 引入），2字节，0 到 65,535
            |——char32_t ，32 位 Unicode 字符类型（C++11 引入），4字节，0 到 4,294,967,295
            |——long long ，8位长长整型（C++11 引入）
            |——auto   ，自动推断类型 🌟✅
        |——数据结构（C++新增）
            |——类：
                关键字class，类是C++中用于面向对象编程的核心结构，允许定义成员变量和成员函数。与struct类似，但功能更强大，支持继承、封装、多态等特性。
                可以包含成员变量、成员函数、构造函数、析构函数。
                支持面向对象特性，如封装、继承、多态。
            |——动态数组（Vector）：
                vector 是 C++ 标准库提供的动态数组实现，可以动态扩展容量，支持随机访问。
                优点：支持随机访问，动态扩展。
                缺点：插入和删除中间元素的效率较低。
        |——基本语法
            |——Fortran、C、Smalltalk 等任意一种语言的编程风格来编写代码
        |——面向对象四大特性
            |——封装（Encapsulation）🌟✅
                |——封装是将数据和方法组合在一起，对外部隐藏实现细节，只公开对外提供的接口。这样可以提高安全性、可靠性和灵活性。
            |——继承（Inheritance）🌟✅
                |——继承是从已有类中派生出新类，新类具有已有类的属性和方法，并且可以扩展或修改这些属性和方法。这样可以提高代码的复用性和可扩展性。
            |——多态（Polymorphism）🌟✅
                |——多态是指同一种操作作用于不同的对象，可以有不同的解释和实现。它可以通过接口或继承实现，可以提高代码的灵活性和可读性。
            |——抽象（Abstraction）🌟✅
                |——抽象是从具体的实例中提取共同的特征，形成抽象类或接口，以便于代码的复用和扩展。抽象类和接口可以让程序员专注于高层次的设计和业务逻辑，而不必关注底层的实现细节。🌟✅
        |——关键字(常用)
            |——基础数据类型关键字，在C的基础上新加的
                |——bool      /只有true和false两种状态，常用于判断
                |——wchar_t   /宽字符型，常用于支持中文，  wchar_t wc = L'中'，2或4字节，视平台
                |——char8_t   /现代Unicode编程， char8_t c8 = u8'好'，UTF-8字符（C++20新增）
                |——char16_t  /UTF-16字符（C++11）
                |——char32_t  /UTF-32字符（C++11）
            |——变量/对象生命周期与存储（Storage & Duration）
                |——auto      /让编译器自动推导类型（C++11后最重要用法） 🌟✅
                |——const     /常量，不可修改,比#define更安全，有类型检查
                |——static    /静态存储（文件内共享、类成员共享、函数内保持值）.注意：类静态成员需类外初始化
                |——mutable   /允许const成员函数修改该成员,类内特殊场景
            |——类与面向对象核心（OOP基础）
                |——class    /定义类（默认private）  class Student { ... }; 🌟✅
                |——private  /私有成员，只有本类和友元能访问🌟✅
                |——public   /公有成员，谁都可以访问🌟✅
                |——protected /受保护成员，本类 + 子类 + 友元可访问 🌟✅
                |——this     /指向当前对象的指针,this->age = 18;  常用于区分同名参数 🌟✅
                |——friend   /友元（允许访问private/protected）;friend class Utils;用得少，慎用破坏封装
                |——virtual  /虚函数（实现多态） ,实现多态的核心  virtual void draw() = 0;🌟✅
                |——override /显式声明这是重写（C++11，推荐加）,防止拼写错误，void draw() override; 🌟✅
                |——final    /禁止继承/禁止重写（C++11） 设计意图明确，class My final {} 🌟✅
                |——explicit /禁止隐式转换（单参数构造函数最常见）,防止意外转换，explicit MyClass(int x);🌟✅
            |——内存管理与指针相关
                |——new和delete /动态分配内存（返回指针）和释放内存。尽量去用智能指针 🌟✅
                |——nullptr    /空指针常量（C++11，推荐替代NULL）,int* ptr = nullptr; 🌟✅
            |——强制类型转换（4种cast，安全性递减）
                |——static_cast   /编译期转换（相关类型、数值转换等）,安全性高，日常最常用🌟✅
                |——dynamic_cast  / 运行时安全向下转型（多态类）,安全性高，继承体系中检查类型🌟✅
                |——const_cast    /加/去const或volatile ，极少用，危险
                |——reinterpret_cast /低级、危险的位模式重新解释（指针间任意转换）,极少用（如硬件交互）安全性极低
            |——不重要
                |——namespace / using：命名空间管理 🌟✅
                |——inline：建议函数内联（现代常用于头文件定义）🌟✅
                |——constexpr（C++11）：编译期常量/函数🌟✅
            |——模板与现代泛型（中高级）
                |——template、typename、concept（C++20）、requires（C++20）
## 学习内容
1.输出和输入  ，cin >> /cout <<
2.命名空间  using namespace std
3.类的创建和使用，对比结构体，类的特点，可单独设置私有化、公有化，以及可以引用函数。类里面构造函数（有参，无参），析构函数~fun()自动释放栈。🌟✅
4.引用  int a = 10; int &ref = a; ref 是 a 的引用。区别于指针，引用必须初始化不为空🌟✅
5.继承：继承允许我们依据另一个类来定义一个类🌟✅
    访问控制和继承：派生类可以访问基类中所有的非私有成员。因此基类成员如果不想被派生类的成员函数访问，则应在基类中声明为 private
    一个派生类继承了所有的基类方法，但下列情况除外：
        基类的构造函数、析构函数和拷贝构造函数。
        基类的重载运算符。
        基类的友元函数。
6.多态：当类之间存在层次结构，并且类之间是通过继承关联时，就会用到多态。🌟✅
7.抽象：即函数接口调用，不必知道内部原理实现
## 代码练习
各.cpp文件

## 遇到问题 & 解决方案
1.STL标准模板库标准模板库待深入学习
2.vector 本质上是一个可自动扩容的动态数组
3.智能指针
-----------------------------------------------------------------
C++ 支持 4 种编程范式，同一件事 N 种写法，选择多 = 灵活但难上手
兼容 C 过程式：你原来写 C 的代码，C++ 能直接跑；
面向对象 OOP：类、封装、继承、多态、虚函数、深浅拷贝（你刚学的难点）；
泛型模板 STL：vector/map 智能指针，模板语法；
函数式：lambda、右值引用、移动语义（Qt 高频）。

C++ 在 C 基础上叠加一整套全新规则，层层叠加
新增基础层：引用、命名空间、cin/cout、const 强化；
OOP 层：构造 / 析构、拷贝构造、深浅拷贝、虚函数、虚析构、this 指针、友元、static 静态；
内存层：new/delete、智能指针 unique_ptr/shared_ptr、移动语义；
STL 容器层：string/vector/map 迭代器、算法；
Qt 额外规则：Q_OBJECT、信号槽、Q_PROPERTY、qml 与 C++ 交互。

分优先级学习，不用全盘吃透
✅ 车载必须精通（手写代码）：
类、封装、构造 / 析构、深浅拷贝、const 引用、unique_ptr/shared_ptr、Q_OBJECT 信号槽、Q_PROPERTY、std::string/vector
⚠️ 暂时浅尝辄止，不用深挖：
复杂模板、模板元编程、高级右值引用、多重虚继承、泛型高级算法，日常 HMI 开发几乎用不到，面试问到再补。
------------------------------------------------------------------

## 重要知识点总结
1、C++ 引用 vs 指针🌟✅
    引用很容易与指针混淆，它们之间有三个主要的不同：
        1.不存在空引用，引用必须连接到一块合法的内存， 必须在创建时被初始化。指针可以在任何时间被初始化。
        2.一旦引用被初始化为一个对象，就不能被指向到另一个对象。指针可以在任何时候指向到另一个对象。
        3.引用的对象必须是一个变量，而指针必须是一个地址。
        4.引用 不占用额外内存（编译器通常将其优化为直接操作所引用的对象）。指针通常占用8字节
        5.引用是直接使用，无需解引用操作符，例如：ref = 10;  指针必须解指针
        6.引用不支持多级间接访问（不能有引用的引用）。指针可以有二级指针等多级访问
        7.不能直接创建引用数组，但可以创建数组的引用，例如：int (&ref)[10] = arr; 指针可以有指针数组和数组指针
    int i = 17;
    int&  r = i;
    声明可以读作 "r 是一个初始化为 i 的整型引用"
2、类（class）-》（可以理解为，类里面的各种变量和函数，进行封装、继承、多态、抽象的特性操作）🌟✅🌟✅🌟✅🌟✅🌟✅
        类是 C++ 的核心特性。类的创建和使用，对比结构体，类的特点，可单独设置私有化、公有化，以及可以引用函数。类里面构造函数（有参，无参），析构函数~fun()自动释放栈。类用于指定对象的形式，是一种用户自定义的数据类型，它是一种封装了数据和函数的组合。类中的数据称为成员变量，函数称为成员函数。类可以被看作是一种模板，可以用来创建具有相同属性和行为的多个对象。
  *类的一大属性：🌟✅继承：继承允许我们依据另一个类来定义一个类，当创建一个类时，您不需要重新编写新的数据成员和成员函数，只需指定新建的类继承了一个已有的类的成员即可。这个已有的类称为基类，新建的类称为派生类。
            // 基类
        class Animal {
            // eat() 函数
            // sleep() 函数
        };

        //派生类
        class Dog : public Animal {
            // bark() 函数
        };
    *访问和控制：派生类可以访问基类中所有的非私有成员。因此基类成员如果不想被派生类的成员函数访问，则应在基类中声明为 private。
        一个派生类继承了所有的基类方法，但下列情况除外：
            基类的构造函数、析构函数和拷贝构造函数。
            基类的重载运算符。
            基类的友元函数。
        公有继承（public）：当一个类派生自公有基类时，基类的公有成员也是派生类的公有成员，基类的保护成员也是派生类的保护成员，基类的私有成员不能直接被派生类访问，但是可以通过调用基类的公有和保护成员来访问。
        保护继承（protected）： 当一个类派生自保护基类时，基类的公有和保护成员将成为派生类的保护成员。
        私有继承（private）：当一个类派生自私有基类时，基类的公有和保护成员将成为派生类的私有成员。
    *多继承
        多继承即一个子类可以有多个父类，它继承了多个父类的特性。
        class <派生类名>:<继承方式1><基类名1>,<继承方式2><基类名2>,…
        {
            <派生类类体>
        };
多态：当类之间存在层次结构，并且类之间是通过继承关联时，就会用到多态，C++ 多态允许使用基类指针或引用来调用子类的重写方法，从而使得同一接口可以表现不同的行为。🌟✅🌟✅🌟✅
  多态的几个关键点：
        虚函数（Virtual Functions）：
            在基类中声明一个函数为虚函数，使用关键字virtual。
            派生类可以重写（override）这个虚函数。
            调用虚函数时，会根据对象的实际类型来决定调用哪个版本的函数。
        动态绑定（Dynamic Binding）：
            也称为晚期绑定（Late Binding），在运行时确定函数调用的具体实现。
            需要使用指向基类的指针或引用来调用虚函数，编译器在运行时根据对象的实际类型来决定调用哪个函数。
        纯虚函数（Pure Virtual Functions）：
            一个包含纯虚函数的类被称为抽象类（Abstract Class），它不能被直接实例化。
            纯虚函数没有函数体，声明时使用= 0。
            它强制派生类提供具体的实现。
    多态的实现机制：
        虚函数表（V-Table）：C++运行时使用虚函数表来实现多态。每个包含虚函数的类都有一个虚函数表，表中存储了指向类中所有虚函数的指针。
        虚函数指针（V-Ptr）：对象中包含一个指向该类虚函数表的指针。
  使用多态的优势：
    代码复用：通过基类指针或引用，可以操作不同类型的派生类对象，实现代码的复用。
    扩展性：新增派生类时，不需要修改依赖于基类的代码，只需要确保新类正确重写了虚函数。
    解耦：多态允许程序设计更加模块化，降低类之间的耦合度。
    
    注意事项：
    只有通过基类的指针或引用调用虚函数时，才会发生多态。
    如果直接使用派生类的对象调用函数，那么调用的是派生类中的版本，而不是基类中的版本。
    多态性需要运行时类型信息（RTTI），这可能会增加程序的开销。
抽象：
    抽象把代码分离为接口和实现。所以在设计组件时，必须保持接口独立于实现，这样，如果改变底层实现，接口也将保持不变。在这种情况下，不管任何程序使用接口，接口都不会受到影响，只需要将最新的实现重新编译即可。

    数据抽象有两个重要的优势：
        类的内部受到保护，不会因无意的用户级错误导致对象状态受损。
        类实现可能随着时间的推移而发生变化，以便应对不断变化的需求，或者应对那些要求不改变用户级代码的错误报告。

## 截图
暂无